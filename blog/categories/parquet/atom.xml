<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: parquet | Thoughts Resampled]]></title>
  <link href="http://blog.antlypls.com/blog/categories/parquet/atom.xml" rel="self"/>
  <link href="http://blog.antlypls.com/"/>
  <updated>2016-02-07T22:13:07+01:00</updated>
  <id>http://blog.antlypls.com/</id>
  <author>
    <name><![CDATA[Anatoliy Plastinin]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[How to Write Data into Parquet]]></title>
    <link href="http://blog.antlypls.com/blog/2015/12/02/how-to-write-data-into-parquet/"/>
    <updated>2015-12-02T00:46:12+01:00</updated>
    <id>http://blog.antlypls.com/blog/2015/12/02/how-to-write-data-into-parquet</id>
    <content type="html"><![CDATA[<p>Column oriented data stores have proven its success for many analytical purposes.
Such success was shown by <a href="https://en.wikipedia.org/wiki/RCFile">RCFile</a>
and <a href="https://orc.apache.org/">Apache ORC</a> formats
and their wide adoption in many distributed data processing tools in Hadoop ecosystem.</p>

<p>Another columnar format that has gained popularity lately is
<a href="https://parquet.apache.org/">Apache Parquet</a>,
which is now top level Apache project.
It is supported by many data processing tools including
<a href="http://spark.apache.org/docs/latest/sql-programming-guide.html#parquet-files">Spark</a> and
<a href="https://prestodb.io/overview.html">Presto</a> provide support for parquet format.</p>

<p>Recently I&rsquo;ve been experimenting with storing data in the parquet format,
so I thought it might be a good idea to share a few examples.
This post covers the basics of how to write data into parquet.</p>

<!--more-->


<h2>Non-hadoop writer</h2>

<p>I’m going to show how to implement simple non-hadoop writer.
It will be enough to start experimenting with parquet and its options.
It also might be useful enough in cases when you have to deal with serial data import
and when there is no need for big guns like Spark.</p>

<p>To write data in parquet we need to define a schema.
And fortunately parquet provides support for popular data serialization libraries,
like <a href="https://avro.apache.org/">avro</a>,
<a href="https://developers.google.com/protocol-buffers/">protocol buffers</a>
and <a href="https://thrift.apache.org/">thrift</a>.
For demo purposes I simply use protobuf.</p>

<p>Please, do not be confused, protobuf is a serialization library,
but here it&rsquo;s used only to define record with schema.
So we can use <code>ProtoParquetWriter</code> that comes with parquet out-of-the-box.
Parquet doesn’t use serialization functionality of any of those libraries, it has its own binary format.</p>

<p>Frankly, in most cases protobuf is not the best choice for defining record schema, since it doesn’t has many types that parquet provides, like <code>DECIMAL</code> or <code>INT96</code> for timestamps.</p>

<p>If you are interested in low level details of how to write custom data classes checkout following
<a href="https://github.com/apache/parquet-mr/tree/apache-parquet-1.8.1/parquet-column/src/main/java/org/apache/parquet/example">examples</a>
and <a href="https://github.com/apache/parquet-mr/tree/apache-parquet-1.8.1/parquet-benchmarks/src/main/java/org/apache/parquet/benchmarks">benchmarks</a>
from parquet repository.</p>

<p>Suppose we have an <code>Event</code> class generated from protobuf definition, so we can write collection of events like this</p>

<pre><code class="scala">val compressionCodecName = CompressionCodecName.GZIP
val blockSize = 256 * 1024 * 1024
val pageSize = 1 * 1024 * 1024
val outputPath = new Path("data.parquet")

val parquetWriter = new ProtoParquetWriter[Event](outputPath, classOf[Event], compressionCodecName, blockSize, pageSize)

events.foreach(parquetWriter.write)
parquetWriter.close()
</code></pre>

<p>Page size is an amount of data buffered before it’s written as page.
This setting might affect compression performance.
Block (row group) size is an amount of data buffered in memory before it is written to disc.
For more details about what pages and row groups are,
please see <a href="https://github.com/apache/parquet-format/blob/master/README.md">parquet format documentation</a>.</p>

<h2>Writing parquet files to S3</h2>

<p>Now let’s see how to write parquet files directly to <a href="https://aws.amazon.com/s3/">Amazon S3</a>.
This can be done using <a href="https://wiki.apache.org/hadoop/AmazonS3">Hadoop S3 file systems</a>.
More precisely, here we’ll use S3A file system.
It will require a few code changes, we’ll use <code>ParquetWriter</code> class to be able to pass <code>conf</code> object with AWS settings.</p>

<pre><code class="scala">val compressionCodecName = CompressionCodecName.GZIP
val blockSize = 256 * 1024 * 1024
val pageSize = 1 * 1024 * 1024
val outputPath = new Path("s3a://bucket/data.parquet")

val accessKey = "aws_access_key"
val secretKey = "aws_secret_key"
val conf = new Configuration
conf.set("fs.s3a.access.key", accessKey)
conf.set("fs.s3a.secret.key", secretKey)

val writeSupport = new ProtoWriteSupport[Event](classOf[Event])

val parquetWriter = new ParquetWriter[Event](outputPath,
  writeSupport, compressionCodecName, blockSize, pageSize, pageSize,
  ParquetWriter.DEFAULT_IS_DICTIONARY_ENABLED, ParquetWriter.DEFAULT_IS_VALIDATING_ENABLED,
  ParquetWriter.DEFAULT_WRITER_VERSION,
  conf
)
</code></pre>

<p>Now you should have enough information to start experimenting with parquet,
you can find examples from this post as a single project in
<a href="https://github.com/antlypls/parquet-demo">this repository</a>.</p>
]]></content>
  </entry>
  
</feed>
